NEXT_PUBLIC_SITE_NAME=LAHTHA
NEXT_PUBLIC_API_URL=http://localhost:3000

# Server-side base URL of the lahtha-click backend, used by the Next API route
# proxies (never exposed to the browser).
BACKEND_URL=http://localhost:3000

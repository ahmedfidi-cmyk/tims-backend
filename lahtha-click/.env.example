NODE_ENV=development
PORT=3000

# MongoDB
MONGO_URI=mongodb://localhost:27017
MONGO_DB_NAME=lahtha_click_dev

# Logging
LOG_LEVEL=info
SERVICE_NAME=lahtha-click

# IAM — identity & sessions
# OTP codes are stored only as HMAC(code, pepper). Override with a real secret
# outside local dev (the default is intentionally insecure).
IAM_OTP_PEPPER=dev-otp-pepper-change-me

# Microsoft Entra ID for MFA step-up (elevated/CLICK actions).
# When unset, MFA step-up fails closed.
# ENTRA_TENANT_ID=
# ENTRA_CLIENT_ID=
# ENTRA_ISSUER=

# Payments (ADR-0007). Outside production the default provider is the dev
# "stub" adapter, which AUTO-CAPTURES every intent — it MUST NEVER be enabled
# in production. Real providers (tabby/tamara/moyasar) fail closed until both
# their API key and webhook secret are configured.
# PAYMENT_PROVIDER=stub
# TABBY_API_KEY=
# TABBY_WEBHOOK_SECRET=
# TAMARA_API_KEY=
# TAMARA_WEBHOOK_SECRET=
# MOYASAR_API_KEY=
